#!/bin/bash

# Hains - Curator Agent Wrapper
# Launches claude with the curator system prompt

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
SYSTEM_PROMPT=$(cat "$SCRIPT_DIR/SYSTEM_PROMPT.md")
exec claude --system-prompt "$SYSTEM_PROMPT" "$@"
